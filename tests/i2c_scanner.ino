#include <Wire.h> #define I2C_SDA 21 #define I2C_SCL 22 void setup() { Serial.begin(115200); delay(1000); Wire.begin(I2C_SDA, I2C_SCL); Serial.println(); Serial.println("=============================="); Serial.println("I2C Scanner startet..."); Serial.println("=============================="); } void loop() { byte error; int nDevices = 0; Serial.println(); Serial.println("Suche I2C Geraete..."); for (byte address = 1; address < 127; address++) { Wire.beginTransmission(address); error = Wire.endTransmission(); if (error == 0) { Serial.print("Gefunden bei Adresse: 0x"); if (address < 16) { Serial.print("0"); } Serial.println(address, HEX); nDevices++; } } if (nDevices == 0) { Serial.println("Keine I2C Geraete gefunden!"); } else { Serial.print("Anzahl gefundene Geraete: "); Serial.println(nDevices); } Serial.println("------------------------------"); delay(3000); }
